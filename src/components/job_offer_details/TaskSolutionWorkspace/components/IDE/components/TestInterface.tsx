import React from "react";

const TestInterface = ({testsData, testFile}) => {
    return(
        <div className="flex">
           <button>Test all</button>
        </div>
    )
}

export default TestInterface;