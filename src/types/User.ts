type User = {
  id: string;
  username: string;
  email: string;
  credits: number;
  isCompany: boolean;
  description: string;
  // imageSmall: string;
  // imageBig: string;
  role: string;
  imageUrl: string;
};

export default User;
